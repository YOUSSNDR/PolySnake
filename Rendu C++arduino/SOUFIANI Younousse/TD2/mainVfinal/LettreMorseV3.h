class LettreMorse {
    public://on met en publique ce a quoi on veut pouvoir accéder
      LettreMorse(); //constructeur
      void morsetr(int code); //on défini la fonction morsetr de la classe
      const char* morse; //on définit la variable morse de la classe
    private:
};